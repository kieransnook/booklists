import React from "react";
import "./style.css"

function Header() {
    return (
        <header >
            <h1> Google Books Search</h1>
            <p>Search for and Save Books you may want to read!</p>
        </header>
    );
}

export default Header;